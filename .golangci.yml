run:
  timeout: 5m
  go: '1.25'

linters:
  disable-all: true
  enable:
    - errcheck
    - govet
    - gofmt
    - ineffassign
    - staticcheck

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
    - path: mock_
      linters:
        - errcheck